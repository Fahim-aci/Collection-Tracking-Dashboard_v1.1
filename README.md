
  # Collection Tracking Dashboard

  This is a code bundle for Collection Tracking Dashboard. The original project is available at https://www.figma.com/design/d9UIr3DsiodTSLSxwrCNoq/Collection-Tracking-Dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # Collection-Tracking-Dashboard_v1.1
